# Copy to .env (never commit .env)
# Use VITE_DATABASE_URL — this is the only supported variable name.
# DATABASE_URL without the VITE_ prefix is NOT exposed to the browser.
VITE_DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
