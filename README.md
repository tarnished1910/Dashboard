# Dashboard

Set your Neon connection string before running:

```bash
VITE_DATABASE_URL="postgresql://..." npm run dev
```

You can also use `DATABASE_URL` if your environment already provides that variable.
