# Dashboard

Set your Neon connection string before running:

```bash
VITE_DATABASE_URL="postgresql://..." npm run dev
```

If you already have `DATABASE_URL`, this project supports it too (see `envPrefix` in `vite.config.ts`).
